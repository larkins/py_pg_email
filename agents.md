Goal: 
this project is to create a small local mail server from scratch.

The mail server will have an API to allow easy local queries and data retrieval without using SMTP

The tech stack is: 
-python
-postgres

Rules: 
-maintain a todo list when you are executing a plan in coding_agent/todo_list.txt so that I can see where you got to if there is an interruption to the llm server
-use tab for indentation in python files
-never use git bash commands without asking permission first
-never use npx or npm bash commands
-do not fetch and run executeables from the internet (eg. via curl or wget) without getting permission first, and not without checking a hash or checksum
